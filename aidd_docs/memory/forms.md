# Forms

How forms are built and validated across the UI.

## Approach

- No dedicated complex form library identified. Likely utilizing native HTML forms for simple interactions (like contact forms) if any.

## Conventions

- Native HTML5 validation attributes (`required`, `type="email"`, etc.).
